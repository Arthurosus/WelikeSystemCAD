# ---------- conexão central -------------
DB_USER=root
DB_PASS=SUASENHA
DB_HOST=localhost
DB_PORT=3306
CENTRAL_DB=central_system          # nome do banco “mestre”

# string-root (sem nome de BD) – usada para criar sub-bancos
DATABASE_URL_ROOT=mysql+pymysql://${DB_USER}:${DB_PASS}@${DB_HOST}

# jwt
JWT_SECRET_KEY=super-secret
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60
